# Hello World in Perl

## Run locally

```bash
perl hello.pl
```

## Run with Docker

```bash
docker build -t hello-perl .
docker run --rm hello-perl
```

`use strict` and `use warnings` enable useful safety checks. `print` writes the message to standard output.
