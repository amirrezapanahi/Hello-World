# Hello World in Ruby

## Run locally

```bash
ruby hello.rb
```

## Run with Docker

```bash
docker build -t hello-ruby .
docker run --rm hello-ruby
```

Ruby's `puts` method writes the text followed by a newline.
