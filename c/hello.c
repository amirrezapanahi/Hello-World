#include <stdio.h>  // Declares the printf function.

int main(void) {
    // printf writes text to standard output; \n starts a new line.
    printf("Hello, World!\n");

    // Zero tells the operating system that the program succeeded.
    return 0;
}
