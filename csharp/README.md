# Hello World in C#

## Run locally

```bash
dotnet run
```

## Run with Docker

```bash
docker build -t hello-csharp .
docker run --rm hello-csharp
```

This project uses top-level statements. `Console.WriteLine` writes the message to standard output.
