# Hello World in PHP

## Run locally

```bash
php hello.php
```

## Run with Docker

```bash
docker build -t hello-php .
docker run --rm hello-php
```

This is a PHP command-line example; the source explains `echo` and `PHP_EOL`.
