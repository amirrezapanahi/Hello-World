# Hello World in Rust

## Run locally

```bash
cargo run
```

## Run with Docker

```bash
docker build -t hello-rust .
docker run --rm hello-rust
```

`main` is the entry point, and the `println!` macro writes a complete line to standard output.
