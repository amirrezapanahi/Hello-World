# Hello World in Go

## Run locally

```bash
go run hello.go
```

Or build an executable:

```bash
go build -o hello .
./hello
```

## Run with Docker

```bash
docker build -t hello-go .
docker run --rm hello-go
```
