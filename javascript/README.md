# Hello World in JavaScript (Node.js)

## Run locally

```bash
node hello.js
```

## Run with Docker

```bash
docker build -t hello-javascript .
docker run --rm hello-javascript
```

`console.log` writes text to Node.js standard output.
